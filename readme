# Filter block SUPPORT and NOT
- EC2/Networking/Infra resources (VPC, Subnet, SG, Instance, Volume) → mostly filter { name = "..." value = "..." } style.
- IAM/Identity resources (Users, Roles, Groups, Policies) → name_regex / path_prefix style, koi generic filter block nahi.
